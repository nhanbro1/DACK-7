{
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "nodemon server.js",
    "start-auth": "json-server --watch db.json"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "nodemon": "^2.0.18"
  }
}
